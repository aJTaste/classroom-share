# 練習１
# 「py4-1」を改良して、下の穴埋めを参考にしながら、入力された点数が８０以上の場合「よくがんばりました」と表示するプログラムを作成しましょう。
- （py4-1を上書きする。念のため-modとして改良前・改良後を残している。正式にはpy4-1.py）

## 私
```
# 変数定義
a = int(input("点数を入力してください"))

# 処理
if a >= 80 :
    print("よくがんばりました。")
    
print("お疲れさまでした。")

```

## 先生
```
a = int( input("点数:")

if a >= 80 :
    #条件を満たしている時にする処理
    print("よくがんばりました。")

#条件を満たしていても満たさなくても実行する処理
print("お疲れさまでした。")
```
